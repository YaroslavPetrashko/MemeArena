"use client";

import { SnapDeckBuilder } from "@/components/deck/SnapDeckBuilder";

/** 12-card SNAP deck builder. The legacy 8-card combat builder is removed. */
export default function DeckPage() {
  return <SnapDeckBuilder />;
}
